# vm_legacy — Oracle VM 전용 파일 보관 (retired 2026-08-03)

Oracle VM(144.24.70.224) 은퇴로 비실행 보관된 파일들: systemd 유닛(*.service/*.timer),
deploy.sh(VM 배포기). 맥미니는 `launchd/` 트리를 사용한다.

- 상태 백업: `vm_state_260803.tar.gz` (맥미니 `~/backups/vm_retirement/` · 노트북 `D:\backups\vm_retirement_260803\`)
- 은퇴 직전 태그: `pre-vm-retirement-20260803` · runbook: 메모리 `project_vm_retirement.md`
