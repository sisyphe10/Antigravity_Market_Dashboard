# 환경 변수 및 비밀 정보
.env
*.json
!package.json
!tsconfig.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/

# 가상 환경
venv/
.venv/
bot_env/
calendar_env/
myenv/
test_env/

# 데이터 파일
*.xlsx
*.xlsx.bak*
*.csv
*.db
!dataset.csv
!hotel_adr.csv
!universe_tickers.csv
!Wrap_NAV.xlsx
!자문지/*.xlsx

# 로그
*.log

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# 차트 (charts/ 폴더는 워크플로우에서 커밋)
!charts/
charts/*
!charts/*.png

# JSON (워크플로우에서 커밋하는 파일)
!portfolio_data.json
!fee_revenue.json
!kodex_sectors.json
!featured_data.json
!seibro_tickers.json
!wics_mapping.json
!featured_news.json
!existing_stock_basis.json
!monthly_returns.json
!investor_trading.json
!sources.json
!landing_highlights.json
!landing_quotes.json
!stock_master.json
!disclosures.json
!corp_codes.json
!universe.json
!universe_history.json
!index_history.json
!foreign_ir_sources.json
!wics_all.json
!kofia_stats.json
!contribution_data.json
!dh_codes.json

# 기타
*.bat

# SSH 키
*.key
*.pem

# 시크릿 폴더 (절대 커밋 금지)
토큰/
구글 콘솔 웹 애플리케이션 비번/
PAT/

# 메모/임시
vm-bot.txt
*.png
!charts/*.png

# 일회성 로컬 전용 자료 (git 업로드 제외)
target_transform_chart.html
execution/generate_target_transform_chart.py
db_target_transform_2nd_chart.html
db_target_transform_2nd_chart_nostem.html
execution/generate_db_target_transform_2nd_chart.py

# Research bot 일일 미디어 (텔레그램 첨부)
execution/research_bot/media/

# KIS featured 배치 생성 산출물 (VM에서 재생성)
kis_universe_master.json
kis_price_history.json
newhigh_20d.json
featured_data_kis.json

# 신고가 테스트 전송 (B안)
.newhigh_test_msg.txt
.claude/settings.local.json

# 가계부 거래 알림 상태 (VM 로컬, deploy.sh가 백업)
.ledger_notified.json

# local API key files - never commit
secrets/

# Wrap_NAV push hold marker (local_safe_push)
Wrap_NAV_push.HOLD
!seonyuduo_feedback_tips.json

# 워처 PID — 재시작마다 변함, 추적 시 영구 dirty로 auto_pull ff 차단 → 추적 제외
watch_wrap_nav.pid

# auto_pull.ps1 의 dirty 백업 patch (로컬 전용)
backups/
